import React from 'react';
import { TextMuted } from 'styles';

const NoResults = () => {
	return (
		<div>
			<TextMuted>Looks like there are no matching results. Clear filters and try again :) </TextMuted>
		</div>
	);
};

export default NoResults;
